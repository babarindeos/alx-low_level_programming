#include <stdlib.h>
#include <stdio.h>

/**
 * main - Print single digits
 * Return: 0
 */
int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
		printf("%d", digit);
	}

	printf("\n");

	return (0);
}
